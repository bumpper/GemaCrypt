{
  "name": "gemacrypt-tauri",
  "version": "0.0.1",
  "private": true,
  "description": "Desktop wrapper for GemaCrypt PHP application using Tauri",
  "author": "Dan Neiderhiser",
  "scripts": {
    "start:php": "php -S 127.0.0.1:8000 -t \"F:/Daniel/My Web Sites/radius.center/gemacrypt\"",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "dev": "start /B cmd /C \"php -S 127.0.0.1:8000 -t \"F:/Daniel/My Web Sites/radius.center/gemacrypt\"\" & tauri dev",
    "dev:all": "start /B cmd /C \"php -S 127.0.0.1:8000 -t \"F:/Daniel/My Web Sites/radius.center/gemacrypt\"\" & tauri dev",
    "build": "tauri build",
    "build:windows": "tauri build --target x86_64-pc-windows-msvc",
    "package:php-windows": "node ./scripts/copy-php.js",
    "clean": "cd src-tauri && cargo clean && cd ..",
    "clean:all": "npm run clean && rmdir /s /q node_modules 2>nul || echo Cleaned"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
